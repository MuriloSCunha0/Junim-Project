# Arquivo de exemplo para variáveis de ambiente
# Copie este arquivo para .env e preencha com suas chaves

# Chave da API Groq (opcional, pode ser inserida na interface)
GROQ_API_KEY=your_groq_api_key_here

# URL do Ollama (para fallback local)
OLLAMA_URL=http://localhost:11434

# Modelo padrão para Groq
GROQ_DEFAULT_MODEL=llama3-70b-8192

# Modelo padrão para Ollama
OLLAMA_DEFAULT_MODEL=codellama:34b
